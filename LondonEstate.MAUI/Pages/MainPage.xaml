<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LondonEstate.MAUI.Pages.MainPage"
             Title="Rent Estimate"
             BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#1F1F1F}">

    <!--<ScrollView>
        <Grid Padding="20" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            --><!-- Dashboard Header --><!--
            <Label Grid.Row="0" 
                   Text="Dashboard" 
                   FontSize="28" 
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light=#1F1F1F, Dark=#FFFFFF}" />

            --><!-- Dashboard Grid --><!--
            <Grid Grid.Row="1" ColumnSpacing="15" RowSpacing="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="150" />
                    <RowDefinition Height="150" />
                </Grid.RowDefinitions>

                --><!-- Bookings Card - Primary (Blue) --><!--
                <Frame Grid.Row="0" Grid.Column="0" 
                       BorderColor="Transparent" 
                       CornerRadius="12" 
                       Padding="0"
                       HasShadow="True"
                       BackgroundColor="#0D47A1">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer 
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavigateToBookingsCommand, FallbackValue={x:Null}}" />
                    </Grid.GestureRecognizers>
                    <StackLayout Padding="20" VerticalOptions="Center">
                        <Label Text="Bookings" 
                               FontSize="20" 
                               FontAttributes="Bold"
                               TextColor="White" />
                    </StackLayout>
                </Frame>

                --><!-- Greetings Card - Success (Green) --><!--
                <Frame Grid.Row="0" Grid.Column="1" 
                       BorderColor="Transparent" 
                       CornerRadius="12" 
                       Padding="0"
                       HasShadow="True"
                       BackgroundColor="#28A745">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer 
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavigateToGreetingsCommand, FallbackValue={x:Null}}" />
                    </Grid.GestureRecognizers>
                    <StackLayout Padding="20" VerticalOptions="Center">
                        <Label Text="Greetings" 
                               FontSize="20" 
                               FontAttributes="Bold"
                               TextColor="White" />
                    </StackLayout>
                </Frame>

                --><!-- Instructions Card - Warning (Orange) --><!--
                <Frame Grid.Row="1" Grid.Column="0" 
                       BorderColor="Transparent" 
                       CornerRadius="12" 
                       Padding="0"
                       HasShadow="True"
                       BackgroundColor="#FFC107">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer 
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavigateToInstructionsCommand, FallbackValue={x:Null}}" />
                    </Grid.GestureRecognizers>
                    <StackLayout Padding="20" VerticalOptions="Center">
                        <Label Text="Instructions" 
                               FontSize="20" 
                               FontAttributes="Bold"
                               TextColor="White" />
                    </StackLayout>
                </Frame>

                --><!-- Invoices Card - Info (Cyan) --><!--
                <Frame Grid.Row="1" Grid.Column="1" 
                       BorderColor="Transparent" 
                       CornerRadius="12" 
                       Padding="0"
                       HasShadow="True"
                       BackgroundColor="#17A2B8">
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer 
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavigateToInvoicesCommand, FallbackValue={x:Null}}" />
                    </Grid.GestureRecognizers>
                    <StackLayout Padding="20" VerticalOptions="Center">
                        <Label Text="Invoices" 
                               FontSize="20" 
                               FontAttributes="Bold"
                               TextColor="White" />
                    </StackLayout>
                </Frame>
            </Grid>
        </Grid>
    </ScrollView>-->
</ContentPage>
