@page
@model LondonEstate.Pages.Admin.Flats.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1> @Html.DisplayFor(model => model.Flat.Name)</h1>
<div>            @Html.DisplayFor(model => model.Flat.Address)
</div>
<hr />

<div>
    <dl class="row">
        @* <dt class="col-sm-2">
            Address:
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Flat.Address)
        </dd>
         <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Flat.CheckIn)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Flat.CheckIn)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Flat.CheckOut)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Flat.CheckOut)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Flat.Empty)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Flat.Empty)
        </dd> *@
        
        <dt class="col-sm-2">
            Wifi:
        </dt>

        <dd class="col-sm-10 d-flex align-items-start">
            <div id="wifi" class="flex-grow-1">
                @* @Html.DisplayFor(model => model.Flat.Wifi) *@
                <textarea asp-for="Flat.Wifi" class="form-control" rows="3"></textarea>
            </div>

            <button type="button" class="ms-2 btn btn-primary" onclick="copyElementText('wifi','')">
                Copy
            </button>
        </dd>
        <dt class="col-sm-2">
            Check-in instruction:
        </dt>

        <dd class="col-sm-10 d-flex align-items-start">
            <div id="checkin-instruction" class="flex-grow-1">
                @* @Html.DisplayFor(model => model.Flat.CheckinInstruction) *@
                <textarea asp-for="Flat.CheckinInstruction" class="form-control" rows="15"></textarea>

            </div>
            <button type="button" class="btn btn-primary ms-2" onclick="copyElementText('checkin-instruction','Check‑in instruction')" >
                Copy
            </button>
        </dd>
        @*  <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Flat.VisualGuideUrl)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Flat.VisualGuideUrl)
        </dd>
       <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Flat.Address)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Flat.Address)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Flat.FlatUrl)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Flat.FlatUrl)
        </dd> *@
    </dl>
</div>
<div>
    @* <a asp-page="./Edit" asp-route-id="@Model.Flat.Id">Edit</a> | *@
    <a class="btn btn-primary" asp-page="/Admin/FlatDetail">Back to List</a>
</div>

@section Scripts {
    <script>
        function fallbackCopy(value) {
            const textarea = document.createElement('textarea');
            textarea.value = value;
            textarea.style.position = 'fixed';
            textarea.style.left = '-9999px';
            document.body.appendChild(textarea);
            textarea.select();
            try { document.execCommand('copy'); } catch { /* ignore */ }
            document.body.removeChild(textarea);
        }

        function copyElementText(elementId, label) {
            const el = document.getElementById(elementId);
            const text = el ? el.textContent.trim() : '';
            if (!text) return;
            const payload = label ? `${label}:\n${text}` : text;
            if (navigator.clipboard && navigator.clipboard.writeText) {
                navigator.clipboard.writeText(payload).catch(() => fallbackCopy(payload));
            } else {
                fallbackCopy(payload);
            }
        }

        function copyWifi() {
            const wifiEl = document.getElementById('wifi');
            
            const wifi = wifiEl ? wifiEl.textContent : '';
            if (!wifi) return;
            if (navigator.clipboard && navigator.clipboard.writeText) {
                navigator.clipboard.writeText(wifi).catch(() => fallbackCopy(wifi));
            } else {
                fallbackCopy(wifi);
            }
        }
    </script>
}